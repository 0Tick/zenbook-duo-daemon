[Unit]
Description=Notify Zenbook Duo daemon before sleep
Before=sleep.target

[Service]
Type=oneshot
ExecStart=/bin/timeout 2.5 /bin/bash -c 'echo suspend_start > /tmp/zenbook-duo-daemon.pipe && sleep 0.5'

[Install]
WantedBy=sleep.target
